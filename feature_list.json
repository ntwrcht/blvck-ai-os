{
  "features": [
    {
      "id": "feat-001",
      "name": "Harness plugin",
      "description": "Engineering-harness plugin: vendored harness-creator skill (MIT, attributed) with solo layout byte-compatible with upstream, plus team layout (features/<id>/ directories, date/Jira-keyed IDs, claim fields) and setup/validate/score commands",
      "dependencies": [],
      "status": "done",
      "evidence": "2026-07-05: node --check clean on all 3 scripts. Scratch solo scaffold scored 100/100, exit 0. Scratch team scaffold + session entry scored 100/100; seeded fixtures fired all three finding types (dangling dep feat-999, duplicate slug project-setup, stale claim 34 days) and exited 1. Round-trip re-verified by ./init.sh step 3/3."
    },
    {
      "id": "feat-002",
      "name": "PM OS plugin",
      "description": "PM operating system plugin: pm-os skill (session ritual, vault rules, 19-workflow router), 21 doc/context templates, 7 agent archetype templates, 5 references, setup/validate/score commands",
      "dependencies": [],
      "status": "done",
      "evidence": "2026-07-05: all 37 files written; placeholder convention consistent ({{machine}} vs [human]); plugin.json JSON-valid via ./init.sh step 2/3. Behavioral validation happens at first /blvck-pm:setup run (feat-005)."
    },
    {
      "id": "feat-003",
      "name": "Marketplace packaging",
      "description": "Repo as a Claude Code plugin marketplace: .claude-plugin/marketplace.json listing both plugins, README with install/use/update, MIT LICENSE with WalkingLab third-party notice",
      "dependencies": ["feat-001", "feat-002"],
      "status": "done",
      "evidence": "2026-07-05: marketplace.json and both plugin.json parse clean (./init.sh step 2/3); install commands documented in README."
    },
    {
      "id": "feat-004",
      "name": "Repo self-harness",
      "description": "This repo runs its own solo harness: CLAUDE.md working rules, feature_list.json, progress.md, and an init.sh that syntax-checks scripts, JSON-validates everything, and runs the solo+team scaffold/validate round-trip including negative-path findings",
      "dependencies": ["feat-001"],
      "status": "done",
      "evidence": "2026-07-05: ./init.sh passes end to end (see progress.md for output summary)."
    },
    {
      "id": "feat-005",
      "name": "Install smoke test",
      "description": "First real installation: /plugin marketplace add ~/blvck-ai-os, install both plugins, run /blvck-harness:setup in a code repo and /blvck-pm:setup in a vault, and fix any friction found",
      "dependencies": ["feat-003"],
      "status": "done",
      "evidence": "2026-07-15: marketplace add + install blvck-harness@blvck-ai-os both exit 0. `claude plugin details` renders displayName 'blvck · harness', version 1.1.0, 5 skills, 0 hooks/MCP/LSP, ~392 tok always-on. Scaffold from the installed plugin path: create-harness.mjs exit 0, all 5 artifacts written, the scaffolded repo's own ./init.sh exits 0, validate-harness.mjs scores 100/100 exit 0. FRICTION FOUND (fixed in feat-008): every skill reference/template Read was permission-denied because plugin files live outside the user's workspace — user-confirmed symptom, root-caused via --output-format stream-json, fixed with allowed-tools and re-verified interactively by the user after /reload-plugins."
    },
    {
      "id": "feat-006",
      "name": "Migration commands",
      "description": "Generic /blvck-harness:migrate and /blvck-pm:migrate: role-based scan of any existing setup (no assumed repo shape), reflect-back confirmation of the reading, source→destination plan approved before any write, additive apply behind a checkpoint, per-group move-to-backup cleanup (never delete), then validate",
      "dependencies": ["feat-001", "feat-002"],
      "status": "done",
      "evidence": "2026-07-05: both migrate.md commands written; README command tables + usage sections and pm-os SKILL.md no-vault path updated; ./init.sh passes (prompt-driven commands — no script or template changes, round-trip unaffected). Behavioral validation folds into feat-005's smoke test."
    },
    {
      "id": "feat-007",
      "name": "Rebrand to blvck",
      "description": "Rename marketplace ai-system → blvck-ai-os and plugins harness → blvck-harness, pm-os → blvck-pm: plugin directories, all three manifests, command prefixes in every doc/command/template, README, CLAUDE.md, trackers, and the repo folder itself (~/ai-system → ~/blvck-ai-os)",
      "dependencies": ["feat-003"],
      "status": "done",
      "evidence": "2026-07-05: git mv of both plugin dirs; ordered global replace + manifest name fields; grep confirms zero stale ai-system//harness://pm-os: references (ai-native-pm-os provenance mentions intentionally kept); ./init.sh passes end to end after rename. Folder rename done last, after commit."
    },
    {
      "id": "feat-008",
      "name": "Official-grade packaging",
      "description": "Close the gap between 'passes claude plugin validate' and 'publishable': fix the permission wall that made skill references/templates unreadable, gate the write commands from model invocation, establish semver release discipline (bump + CHANGELOG + tag) so updates actually reach installed users, restore GitHub MIT license detection, and add CI + community files + repo discovery metadata",
      "dependencies": ["feat-003", "feat-005"],
      "status": "done",
      "evidence": "2026-07-15: (1) PERMISSION WALL — root cause of user-reported 'references and templates not proactive': plugin skill files live outside the user's workspace, so every Read was denied ('you haven't granted it yet'); relative links were never the problem, they resolve correctly. Fixed with allowed-tools: Read(${CLAUDE_PLUGIN_ROOT}/**) on both SKILL.md; user verified interactively after /reload-plugins. NOTE: any skill with allowed-tools fails to launch under `claude -p` regardless of permission mode (proved with a local canary skill), so this is untestable headlessly. Also fixed bare `node scripts/*.mjs` -> ${CLAUDE_SKILL_DIR}/scripts/ in harness SKILL.md (bare paths resolve against the user's cwd, where the script does not exist). (2) VERSION TRAP — 1.0.0 was pinned while 5 commits shipped, so installed users could never receive the rebrand; bumped both to 1.1.0, added per-plugin CHANGELOG.md and a Release Checklist in CLAUDE.md. (3) Gated setup+migrate with disable-model-invocation (they write files); validate+score stay model-invocable. Token saving from gating NOT confirmed — `plugin details` still counts them in always-on. (4) LICENSE restored to verbatim MIT (GitHub reported 'Other' because of the appended notice); attribution moved verbatim to NOTICE. (5) CI: .github/workflows/validate.yml runs init.sh + `claude plugin validate --strict` (verified it needs no auth: clean HOME/empty env exits 0; broken manifest exits 1) plus a guard that fails if a marketplace entry pins version. (6) CONTRIBUTING.md, SECURITY.md, repo topics + homepage. Verified: ./init.sh passes; all 3 manifests pass --strict; scaffold round-trip 100/100. CAVEAT: `claude plugin validate --strict` PASSED frontmatter that broke the skill at runtime — schema validation is not a substitute for invocation."
    }
  ]
}
